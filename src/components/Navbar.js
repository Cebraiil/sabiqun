import React from 'react'

const Navbar = () => {
  return (
    <header>
        <nav>
            <a href="#">Home</a>
            <a href="#">eymen</a>
            <a href="#">Explore</a>
        </nav>
  </header>
  )
}

export default Navbar